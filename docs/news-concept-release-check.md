# Focused News Concept Release Check

The local `/news-concept` route rendered with live WordPress article data, a compact World Elections globe module, and a Selma Historical Atlas feature. On the first two live-domain checks immediately after checkpoint `81c5d9bc`, the published domain still returned the previous application's Not Found screen for `/news-concept`.

After cache-busting refresh and deployment propagation, the live route loaded correctly. The published page shows the current Black Politics Now lead report and latest articles, the World Elections globe slot, and the Selma Historical Atlas feature with the National Park Service bridge image. The broad `/site-concept` direction is no longer linked from the application; the focus is solely the News-page concept.
