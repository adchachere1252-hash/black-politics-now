import { Calendar, CheckCircle2, ExternalLink, Landmark, Scale, Vote } from "lucide-react";
import { getCompletedWorldResults, getWorldReferendums, type WorldElectionRecord } from "@/lib/worldLegacyViews";

const displayDate = (value: string) => new Date(`${value}T00:00:00`).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });
const toSourceLinks = (value?: string | null, notes?: string | null) => {
  try {
    const parsed = value ? JSON.parse(value) : [];
    const listed = Array.isArray(parsed) ? parsed.filter((item) => typeof item === "string") : [];
    const inNotes = ((notes || "").match(/https:\/\/[^\s|)]+/g) || []);
    return Array.from(new Set([...listed, ...inNotes]));
  } catch { return ((notes || "").match(/https:\/\/[^\s|)]+/g) || []); }
};

function ResultCard({ election, onSelect }: { election: WorldElectionRecord; onSelect: (election: WorldElectionRecord) => void }) {
  const sources = toSourceLinks(election.sourceUrls, election.notes);
  return <article className="rounded-2xl border border-border bg-card/85 p-4 shadow-sm transition-colors hover:border-primary/35">
    <div className="flex items-start gap-3"><img src={`https://flagcdn.com/w80/${election.countryCode.toLowerCase()}.png`} className="mt-0.5 h-9 w-9 rounded-full object-cover" alt="" /><div className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-2"><p className="font-semibold text-foreground">{election.country}</p><span className="inline-flex items-center gap-1 rounded-full border border-emerald-500/25 bg-emerald-500/10 px-2 py-0.5 text-[10px] font-bold uppercase tracking-wide text-emerald-700 dark:text-emerald-300"><CheckCircle2 size={11}/> Verified result</span></div><p className="mt-0.5 truncate text-xs text-muted-foreground">{election.electionName}</p></div></div>
    <div className="mt-4 rounded-xl border border-emerald-500/15 bg-emerald-500/[0.045] p-3"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-emerald-700 dark:text-emerald-300">Recorded outcome</p><p className="mt-1 text-sm font-semibold text-foreground">{election.winner || "Outcome recorded"}{election.winnerParty ? ` · ${election.winnerParty}` : ""}</p><div className="mt-2 flex flex-wrap gap-x-3 gap-y-1 text-[11px] text-muted-foreground"><span className="inline-flex items-center gap-1"><Calendar size={11}/>{displayDate(election.electionDate)}</span>{election.turnoutPct ? <span>{election.turnoutPct}% turnout</span> : null}{election.totalVotes ? <span>{Number(election.totalVotes).toLocaleString()} votes</span> : null}</div></div>
    <div className="mt-3 flex items-center justify-between gap-2"><button onClick={() => onSelect(election)} className="inline-flex items-center gap-1 text-xs font-semibold text-primary hover:underline"><Landmark size={13}/> Open country record</button>{sources[0] ? <a href={sources[0]} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-xs font-semibold text-primary hover:underline">Source <ExternalLink size={12}/></a> : <span className="text-[11px] text-muted-foreground">Evidence link unavailable</span>}</div>
  </article>;
}

export function WorldResultsView({ elections, onSelect }: { elections: WorldElectionRecord[]; onSelect: (election: WorldElectionRecord) => void }) {
  const completed = getCompletedWorldResults(elections);
  return <section className="relative container pb-14"><div className="rounded-2xl border border-border bg-card/65 p-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-primary text-xs font-semibold uppercase tracking-[0.2em]">Verified outcomes</p><h2 className="mt-2 font-display text-2xl font-bold">World Results</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Completed elections with recorded outcomes. Every card links back to the country record and its available source evidence.</p></div><span className="rounded-full border border-primary/25 bg-primary/10 px-3 py-1.5 text-sm font-semibold text-primary">{completed.length} completed</span></div><div className="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{completed.map((election) => <ResultCard key={election.id} election={election} onSelect={onSelect}/>)}</div>{!completed.length && <p className="py-10 text-center text-sm text-muted-foreground">No completed source-backed world results are available yet.</p>}</div></section>;
}

export function WorldReferendumsView({ elections, onSelect }: { elections: WorldElectionRecord[]; onSelect: (election: WorldElectionRecord) => void }) {
  const referendums = getWorldReferendums(elections);
  return <section className="relative container pb-14"><div className="rounded-2xl border border-border bg-card/65 p-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-primary text-xs font-semibold uppercase tracking-[0.2em]">Direct democracy record</p><h2 className="mt-2 font-display text-2xl font-bold">World Referendums</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">A dedicated record of national referendum events, with confirmed results distinguished from future votes and source links kept with each country record.</p></div><span className="rounded-full border border-primary/25 bg-primary/10 px-3 py-1.5 text-sm font-semibold text-primary">{referendums.length} tracked</span></div><div className="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{referendums.map((election) => { const complete = election.status === "Completed"; const source = toSourceLinks(election.sourceUrls, election.notes)[0]; return <article key={election.id} className="rounded-2xl border border-border bg-background/60 p-4"><div className="flex gap-3"><img src={`https://flagcdn.com/w80/${election.countryCode.toLowerCase()}.png`} className="h-9 w-9 rounded-full object-cover" alt=""/><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><p className="truncate font-semibold text-foreground">{election.country}</p><span className={`rounded-full border px-2 py-0.5 text-[10px] font-bold ${complete ? "border-emerald-500/25 bg-emerald-500/10 text-emerald-700 dark:text-emerald-300" : "border-primary/25 bg-primary/10 text-primary"}`}>{complete ? "Completed" : election.status}</span></div><p className="mt-0.5 line-clamp-2 text-xs text-muted-foreground">{election.electionName}</p></div></div><div className="mt-4 flex items-center gap-2 text-[11px] text-muted-foreground"><Vote size={13}/><span>{displayDate(election.electionDate)}</span></div>{complete && <div className="mt-3 rounded-lg border border-emerald-500/15 bg-emerald-500/[0.045] px-3 py-2"><p className="text-[10px] font-bold uppercase tracking-wide text-emerald-700 dark:text-emerald-300">Recorded outcome</p><p className="mt-1 text-xs font-semibold text-foreground">{election.winner || "Outcome recorded"}</p></div>}{!complete && <div className="mt-3 rounded-lg border border-primary/15 bg-primary/[0.04] px-3 py-2 text-xs text-muted-foreground">Scheduled status remains subject to the linked source record.</div>}<div className="mt-3 flex items-center justify-between gap-2"><button onClick={() => onSelect(election)} className="inline-flex items-center gap-1 text-xs font-semibold text-primary hover:underline"><Scale size={13}/> Country record</button>{source && <a href={source} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-xs font-semibold text-primary hover:underline">Source <ExternalLink size={12}/></a>}</div></article>; })}</div>{!referendums.length && <p className="py-10 text-center text-sm text-muted-foreground">No source-backed referendum records are available yet.</p>}</div></section>;
}

export function WorldTimelineView({ elections, onSelect }: { elections: WorldElectionRecord[]; onSelect: (election: WorldElectionRecord) => void }) {
  const ordered = [...elections].sort((a, b) => a.electionDate.localeCompare(b.electionDate));
  return <section className="relative container pb-14"><div className="rounded-2xl border border-border bg-card/65 p-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-primary text-xs font-semibold uppercase tracking-[0.2em]">Global calendar</p><h2 className="mt-2 font-display text-2xl font-bold">Election Timeline</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">The complete source-backed World Elections record in chronological order. Select any event to open its country context and evidence links.</p></div><span className="rounded-full border border-primary/25 bg-primary/10 px-3 py-1.5 text-sm font-semibold text-primary">{ordered.length} events</span></div><div className="mt-6 space-y-3">{ordered.map((election) => <button key={election.id} onClick={() => onSelect(election)} className="group flex w-full items-center gap-4 rounded-xl border border-border bg-background/55 p-3 text-left transition-colors hover:border-primary/35 hover:bg-primary/[0.035]"><div className="w-[72px] shrink-0 border-r border-border pr-4 text-center"><p className="text-xs font-bold text-primary">{new Date(`${election.electionDate}T00:00:00`).toLocaleDateString("en-US", { month: "short" })}</p><p className="font-display text-xl font-bold text-foreground">{new Date(`${election.electionDate}T00:00:00`).getDate()}</p></div><img src={`https://flagcdn.com/w80/${election.countryCode.toLowerCase()}.png`} className="h-8 w-8 rounded-full object-cover" alt=""/><div className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-2"><p className="font-semibold text-foreground group-hover:text-primary">{election.country}</p><span className={`rounded-full border px-2 py-0.5 text-[10px] font-bold ${election.status === "Completed" ? "border-emerald-500/25 bg-emerald-500/10 text-emerald-700 dark:text-emerald-300" : election.status === "Voting Today" ? "border-amber-500/30 bg-amber-500/10 text-amber-700 dark:text-amber-300" : "border-primary/25 bg-primary/10 text-primary"}`}>{election.status}</span></div><p className="mt-0.5 truncate text-xs text-muted-foreground">{election.electionName} · {election.electionType}</p></div></button>)}</div></div></section>;
}

export function WorldResultsTicker({ elections, onSelect }: { elections: WorldElectionRecord[]; onSelect: (election: WorldElectionRecord) => void }) {
  const recent = getCompletedWorldResults(elections).slice(0, 6);
  if (!recent.length) return null;
  return <div className="relative container pb-3"><div className="flex items-center gap-3 overflow-x-auto rounded-xl border border-emerald-500/20 bg-emerald-500/[0.045] px-3 py-2 text-xs"><span className="shrink-0 rounded-full bg-emerald-500/15 px-2 py-1 font-bold uppercase tracking-[0.12em] text-emerald-700 dark:text-emerald-300">World results</span>{recent.map((election) => <button key={election.id} onClick={() => onSelect(election)} className="shrink-0 whitespace-nowrap text-muted-foreground hover:text-primary"><span className="font-semibold text-foreground">{election.country}</span>: {election.winner || "Outcome recorded"}</button>)}</div></div>;
}
