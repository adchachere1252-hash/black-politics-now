import { useMemo, useState } from "react";
import { Building2, CalendarDays, Gavel, Layers3, MapPinned, Scale } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { APPORTIONMENT_HISTORY, APPORTIONMENT_YEARS, STATE_CODES } from "@/data/atlasHistory";

export default function Atlas() {
  const { data: states = [], isLoading } = trpc.election.redistricting.useQuery();
  const [selectedCode, setSelectedCode] = useState<string | null>(null);
  const historicalStates = useMemo(() => {
    const activeByName = new Map((states as any[]).map((state) => [state.stateName, state]));
    return Object.keys(APPORTIONMENT_HISTORY).sort().map((stateName) => activeByName.get(stateName) ?? ({ stateCode: STATE_CODES[stateName], stateName, enacted: false, status: "Historical record", method: "Congressional apportionment", delegationBefore: null, projectedImpact: null, litigationNotes: null, reason: null }));
  }, [states]);
  const selected = useMemo(() => historicalStates.find((state: any) => state.stateCode === selectedCode) ?? historicalStates[0], [historicalStates, selectedCode]);
  const history = selected ? APPORTIONMENT_HISTORY[selected.stateName] ?? [1, 1, 1, 1, 1, 1, 1] : [];
  const selectedIsActive = Boolean(selected && (states as any[]).some((state) => state.stateCode === selected.stateCode));
  const enacted = states.filter((state: any) => state.enacted).length;
  const litigation = states.filter((state: any) => state.litigationNotes).length;

  if (isLoading) return <div className="min-h-[70vh] grid place-items-center"><div className="w-9 h-9 rounded-full border-2 border-primary border-t-transparent animate-spin" /></div>;
  return <div className="min-h-screen bg-background text-foreground">
    <section className="border-b border-border bg-[radial-gradient(circle_at_85%_0%,rgba(212,165,82,.14),transparent_33%)]"><div className="container py-10"><p className="text-primary text-xs uppercase tracking-[0.28em] font-semibold">Congressional geography · 1963–present</p><h1 className="font-display text-4xl sm:text-5xl font-bold mt-3">Historical Atlas</h1><p className="text-muted-foreground mt-3 max-w-3xl">Explore the original Atlas’s 50-state apportionment record alongside the live 2026 redistricting watchlist, keeping long-run history distinct from current legal and map-status reporting.</p><div className="grid grid-cols-2 sm:grid-cols-4 gap-3 mt-7 max-w-3xl"><Metric value={historicalStates.length} label="State histories" icon={MapPinned}/><Metric value={states.length} label="Active watchlist" icon={Layers3}/><Metric value={litigation} label="With litigation" icon={Gavel}/><Metric value="1963–2025" label="Historical span" icon={CalendarDays}/></div></div></section>
    <section className="container py-8 grid xl:grid-cols-[270px_minmax(0,1fr)] gap-6">
      <aside className="rounded-2xl border border-border bg-card overflow-hidden h-fit"><div className="p-4 border-b border-border"><p className="font-semibold">All state histories</p><p className="text-xs text-muted-foreground mt-1">The 50-state apportionment record; active 2026 watchlist entries retain their current map context.</p></div><div className="max-h-[580px] overflow-y-auto divide-y divide-border">{historicalStates.map((state: any) => <button onClick={() => setSelectedCode(state.stateCode)} key={state.stateCode} className={`w-full p-3 text-left flex items-center justify-between transition-colors ${selected?.stateCode === state.stateCode ? "bg-primary/10 text-primary" : "hover:bg-muted/60"}`}><span className="text-sm font-medium">{state.stateName}</span><span className="text-[10px] rounded-full border border-border px-2 py-0.5 text-muted-foreground">{(states as any[]).some((live) => live.stateCode === state.stateCode) ? "2026 watch" : "History"}</span></button>)}</div></aside>
      {selected && <main className="space-y-6"><div className="rounded-2xl border border-border bg-card p-5 sm:p-7"><div className="flex flex-col sm:flex-row justify-between gap-4"><div><p className="text-primary text-xs tracking-[.18em] uppercase">{selected.stateCode} · {selectedIsActive ? "2026 watchlist" : "historical record"}</p><h2 className="font-display text-3xl font-bold mt-2">{selected.stateName}</h2><p className="text-muted-foreground mt-2 max-w-2xl">{selected.reason || "A 1963–2025 congressional apportionment record from the original Historical Atlas."}</p></div><div className="rounded-xl border border-border bg-background p-3 h-fit text-sm"><span className="text-muted-foreground block text-xs">{selectedIsActive ? "Current map status" : "Coverage"}</span><strong>{selectedIsActive ? selected.status || "Monitoring" : "Historical apportionment"}</strong></div></div><div className="grid md:grid-cols-3 gap-3 mt-6"><Info label="Method" value={selected.method || "Congressional apportionment"} icon={Scale}/><Info label="Delegation before" value={selected.delegationBefore || "See seat history"} icon={Building2}/><Info label="Projected impact" value={selected.projectedImpact || "No active 2026 map change"} icon={Layers3}/></div></div>
        <div className="rounded-2xl border border-border bg-card p-5 sm:p-7"><div className="flex justify-between items-end"><div><h3 className="font-display text-xl font-bold">Apportionment history</h3><p className="text-sm text-muted-foreground mt-1">House seats following each decennial redistricting cycle, retained from the original Atlas coverage.</p></div><span className="text-primary font-display text-2xl font-bold">{history[history.length - 1]}</span></div><SeatChart values={history}/><div className="grid grid-cols-7 gap-1 mt-2 text-[10px] text-muted-foreground text-center">{APPORTIONMENT_YEARS.map((year) => <span key={year}>{year}</span>)}</div></div>
        <div className="grid lg:grid-cols-2 gap-6"><div className="rounded-2xl border border-border bg-card p-5"><h3 className="font-display text-xl font-bold">Current delegation grid</h3><p className="text-sm text-muted-foreground mt-1">A compact district index for the current apportionment—not a substitute for the legal district boundary file.</p><div className="grid grid-cols-6 sm:grid-cols-8 gap-2 mt-5">{Array.from({ length: history.at(-1) || 1 }).map((_, index) => <div key={index} className="aspect-square rounded-md border border-primary/25 bg-primary/10 grid place-items-center text-xs font-semibold text-primary">{index + 1}</div>)}</div></div><div className="rounded-2xl border border-border bg-card p-5"><h3 className="font-display text-xl font-bold">Redistricting notes</h3><p className="text-sm text-muted-foreground mt-3 leading-relaxed">{selected.litigationNotes || (selectedIsActive ? "No litigation notes are currently tracked for this state." : "This state is outside the active 2026 watchlist; the Atlas retains its congressional-apportionment history.")}</p><p className="text-sm text-muted-foreground mt-4 leading-relaxed">{selected.reason || "Boundary-era and party-control timelines remain available as the next repository-backed Atlas expansion."}</p></div></div>
      </main>}
    </section>
  </div>;
}

function Metric({ value, label, icon: Icon }: { value: string | number; label: string; icon: any }) { return <div className="rounded-xl border border-border bg-card/80 px-4 py-3"><Icon size={16} className="text-primary mb-2"/><div className="font-display text-xl font-bold">{value}</div><div className="text-[10px] text-muted-foreground uppercase tracking-wide">{label}</div></div>; }
function Info({ label, value, icon: Icon }: { label: string; value: string; icon: any }) { return <div className="rounded-lg border border-border bg-background p-3"><Icon size={15} className="text-primary mb-2"/><p className="text-[10px] uppercase tracking-wide text-muted-foreground">{label}</p><p className="text-sm font-medium mt-1">{value}</p></div>; }
function SeatChart({ values }: { values: number[] }) { const max = Math.max(...values, 1); return <div className="h-52 mt-6 flex items-end gap-2">{values.map((value, index) => <div key={index} className="flex-1 h-full flex flex-col justify-end"><span className="text-xs text-center text-muted-foreground mb-1">{value}</span><div className="rounded-t-md bg-primary/75 min-h-2 transition-all" style={{ height: `${Math.max(8, (value / max) * 100)}%` }} /></div>)}</div>; }
